"""No Django admin for calendar_sync.

Everything is managed through the app's own UI — Settings → Calendar for
subscriptions and invite configuration, and the planning page for sending
invites. There are deliberately no admin registrations here.
"""
